import { crearClienteNavegador } from "./supabase-browser";
export async function isFeatureEnabled(key:string, userId?:string){ const client=crearClienteNavegador(); const {data}=await client.from("feature_flags_app").select("habilitada,porcentaje_rollout,versiones_permitidas").eq("clave",key).maybeSingle(); if(!data?.habilitada) return false; const version=process.env.NEXT_PUBLIC_APP_VERSION??"0.0.0"; if(data.versiones_permitidas?.length && !data.versiones_permitidas.includes(version)) return false; if(data.porcentaje_rollout>=100) return true; if(!userId) return false; let hash=0; for(const c of `${key}:${userId}`) hash=(hash*31+c.charCodeAt(0))>>>0; return hash%100 < data.porcentaje_rollout; }
