import type { CapacitorConfig } from "@capacitor/cli";
const config: CapacitorConfig = { appId:"com.moviliax.ruumruum.conductor", appName:"Ruum Ruum Conductor", webDir:"cap-shell", server:{ androidScheme:"https" } };
export default config;
